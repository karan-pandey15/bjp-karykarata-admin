# Production / deploy env template (safe to commit — no secrets)
# Copy to `.env.production` on the server or set these in Vercel/hosting UI.

VITE_API_BASE_URL=https://lotus-backend-nine.vercel.app/api
VITE_APP_NAME=BJP Karyakarta
